<p id="notice"><%= notice %></p>

<h1><%= link_to @list.description, edit_list_path(@list) %></h1>

<table>
  <thead>
    <th width=>Tasks</th>
    <th width='10'></th>
  </thead>
  <%= render @tasks %>
</table>

<%= link_to 'New Task', new_list_task_path(@list) %> |
<%= link_to 'Back to Lists', lists_path %>
